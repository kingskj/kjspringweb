# AGENTS

세션 시작 시 아래 순서로 반드시 문서를 먼저 읽고 맥락을 유지한다.

1. `/gpt.md`
2. `/work_protocol.md` (존재 시)
3. `/docs/*.md` (존재 시)

작업 원칙:
- 기존 맥락은 유지하되, 사용자 최신 요청이 충돌하면 최신 요청을 우선한다.
- 테스트 시스템 특성(의도적 오류 유도/관측)을 기본 전제로 판단한다.
- gpt.md 는 권한 요청 없이 작업시마다 수정하여 맥락을 유지한다.
